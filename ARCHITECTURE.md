# 🏗️ ARCHITECTURE OVERVIEW

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          MERN PORTFOLIO PLATFORM                     │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                             FRONTEND                                  │
│                    (React + Redux + Tailwind)                        │
│                      Port: 5173 (Vite Dev)                           │
└──────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
        ┌───────────▼──┐   ┌───────▼────┐   ┌─────▼────────┐
        │  Public Pages │   │ Auth Pages │   │ Admin Pages  │
        └───────────────┘   └────────────┘   └──────────────┘
        │                   │                │
        │ • Home            │ • Login        │ • Dashboard
        │ • Projects        │ • Signup       │   - Projects CRUD
        │ • Project Detail  │                │   - Skills CRUD
        │ • About           │                │   - Resume Upload
        │ • Contact         │                │   - Profile View
        └─────────┬─────────┴────────────────┴──────────────┘
                  │
                  │ HTTP Requests (Axios)
                  │ JWT Token in Headers
                  │
┌─────────────────▼─────────────────────────────────────────────────┐
│                         API GATEWAY                                │
│                    (Express Middleware)                            │
│                      Port: 5000                                    │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │ CORS         │  │ JWT Verify   │  │ Role Check   │           │
│  │ Middleware   │  │ Middleware   │  │ Middleware   │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└────────────────────────────────────────────────────────────────────┘
                  │
        ┌─────────┼─────────┐
        │         │         │
┌───────▼──┐ ┌───▼────┐ ┌──▼──────┐
│  Auth    │ │ About  │ │ Project │
│  Routes  │ │ Routes │ │ Routes  │
└──────────┘ └────────┘ └─────────┘
│            │          │
│ /signup    │ /about   │ /projects
│ /login     │ /skills  │ /projects/:id
│ /logout    │ /resume  │
│ /profile   │          │
│            │          │
┌────────────▼──────────▼─────────────┐
│         CONTROLLERS LAYER            │
│  (Business Logic & Validation)       │
│                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────┐│
│  │   Auth   │ │  About   │ │ Proj ││
│  │Controller│ │Controller│ │ Ctrl ││
│  └──────────┘ └──────────┘ └──────┘│
└──────────────────────────────────────┘
                  │
        ┌─────────┼─────────┐
        │         │         │
┌───────▼──┐ ┌───▼────┐ ┌──▼──────┐
│  User    │ │ About  │ │ Project │
│  Model   │ │ Model  │ │ Model   │
└──────────┘ └────────┘ └─────────┘
│            │          │
│ • name     │ • name   │ • title
│ • email    │ • role   │ • description
│ • password │ • bio    │ • technologies
│ • role     │ • skills │ • githubLink
│            │ • email  │ • liveLink
│            │ • phone  │ • image
│            │ • location│
│            │ • links  │
│            │ • resume │
│            │          │
└────────────┴──────────┴─────────┘
                  │
┌─────────────────▼──────────────────┐
│      MONGODB DATABASE               │
│      (Document Store)               │
│                                     │
│  Collections:                       │
│  • users                            │
│  • projects                         │
│  • abouts                           │
└─────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. View Projects Flow (FIXED ✅)

```
┌──────────┐
│   USER   │
└────┬─────┘
     │ Clicks "View Projects"
     ▼
┌────────────────────┐
│   Home Component   │
│   <Link to="/      │
│    projects">      │ ← FIXED: Changed from <a href="#projects">
└─────────┬──────────┘
          │
          │ React Router Navigation
          ▼
┌──────────────────────┐
│  Projects Component  │
│  useEffect() fires   │
└──────────┬───────────┘
           │
           │ API Call: GET /api/projects
           ▼
┌──────────────────────┐
│   Backend API        │
│   projectController  │
│   .getAllProjects()  │
└──────────┬───────────┘
           │
           │ Query MongoDB
           ▼
┌──────────────────────┐
│   MongoDB            │
│   projects           │
│   collection         │
└──────────┬───────────┘
           │
           │ Return documents
           ▼
┌──────────────────────┐
│   Frontend           │
│   setState(projects) │
│   Render UI          │
└──────────────────────┘
```

### 2. Skills Management Flow (Dynamic ✅)

```
Admin Dashboard
      │
      ▼
┌─────────────────────┐
│  Skills Section     │
│  • Display skills   │
│  • Add button       │
│  • Delete buttons   │
└──────┬──────────────┘
       │
       ├─── Add Skill ────────────────┐
       │                              │
       │ 1. Click "Add Skill"         │
       │ 2. Modal opens               │
       │ 3. Enter skill name          │
       │ 4. Click Save                │
       │                              │
       │ POST /api/about/skills       │
       │ { skill: "React.js" }        │
       │                              │
       ▼                              │
┌──────────────────────┐             │
│  aboutController     │             │
│  .addSkill()         │             │
│  - Validate          │             │
│  - Check duplicates  │             │
│  - Save to DB        │             │
└──────┬───────────────┘             │
       │                              │
       ▼                              │
┌──────────────────────┐             │
│  MongoDB             │             │
│  abouts.skills       │             │
│  $push: "React.js"   │             │
└──────┬───────────────┘             │
       │                              │
       │ Success Response             │
       ▼                              │
┌──────────────────────┐             │
│  Frontend Update     │             │
│  - setState          │             │
│  - Show toast        │             │
│  - Close modal       │             │
│  - Refresh list      │             │
└──────────────────────┘             │
       │                              │
       └──────────────────────────────┘
       │
       ├─── Delete Skill ──────────────┐
       │                                │
       │ 1. Click (×) on skill badge   │
       │ 2. Confirm dialog             │
       │ 3. User confirms              │
       │                                │
       │ DELETE /api/about/skills      │
       │ { skill: "React.js" }         │
       │                                │
       ▼                                │
┌──────────────────────┐               │
│  aboutController     │               │
│  .deleteSkill()      │               │
│  - Find document     │               │
│  - Remove from array │               │
│  - Save              │               │
└──────┬───────────────┘               │
       │                                │
       ▼                                │
┌──────────────────────┐               │
│  MongoDB             │               │
│  abouts.skills       │               │
│  $pull: "React.js"   │               │
└──────┬───────────────┘               │
       │                                │
       │ Success Response               │
       ▼                                │
┌──────────────────────┐               │
│  Frontend Update     │               │
│  - Update state      │               │
│  - Show toast        │               │
│  - Refresh list      │               │
└──────────────────────┘               │
       │                                │
       ▼                                │
┌──────────────────────┐               │
│  About Page          │               │
│  (Public view also   │               │
│   updates instantly) │               │
└──────────────────────┘               │
       │                                │
       └────────────────────────────────┘
```

### 3. Contact Information Flow (Dynamic ✅)

```
┌─────────────────────┐
│  Contact Component  │
│  useEffect()        │
└──────┬──────────────┘
       │
       │ API Call: GET /api/about
       ▼
┌─────────────────────┐
│  Backend API        │
│  aboutController    │
│  .getAbout()        │
└──────┬──────────────┘
       │
       │ Query MongoDB
       ▼
┌─────────────────────┐
│  MongoDB            │
│  abouts collection  │
│  {                  │
│    email: "..."     │
│    phone: "..."     │
│    location: "..."  │
│    linkedin: "..."  │
│    github: "..."    │
│  }                  │
└──────┬──────────────┘
       │
       │ Return document
       ▼
┌─────────────────────┐
│  Frontend           │
│  setContactInfo()   │
│  Render:            │
│  • Email display    │
│  • Phone display    │
│  • Location display │
│  • Social links     │
└─────────────────────┘
```

### 4. Authentication Flow

```
┌────────────────────┐
│  Login Component   │
│  Form submission   │
└─────────┬──────────┘
          │
          │ POST /api/auth/login
          │ { email, password }
          ▼
┌──────────────────────┐
│  authController      │
│  .login()            │
│  1. Find user        │
│  2. Compare password │
│  3. Generate JWT     │
└──────────┬───────────┘
           │
           │ JWT Token + User Data
           ▼
┌──────────────────────┐
│  Frontend            │
│  Redux Store         │
│  - Save token        │
│  - Save user data    │
│  - Redirect          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Protected Routes    │
│  Check:              │
│  - Token exists?     │
│  - Role = admin?     │
└──────────────────────┘
```

---

## Component Hierarchy

### Frontend Structure

```
App.jsx
│
├─ Router
│  │
│  ├─ Navbar (All pages)
│  │  ├─ Logo/Brand
│  │  ├─ Navigation Links
│  │  └─ Auth Buttons
│  │
│  ├─ Routes
│  │  │
│  │  ├─ Public Routes
│  │  │  ├─ Home
│  │  │  │  ├─ Hero Section
│  │  │  │  ├─ Action Buttons ✅ FIXED
│  │  │  │  └─ Feature Cards
│  │  │  │
│  │  │  ├─ Projects
│  │  │  │  ├─ Projects Grid
│  │  │  │  └─ Project Card
│  │  │  │
│  │  │  ├─ ProjectDetail
│  │  │  │  └─ Full Project Info
│  │  │  │
│  │  │  ├─ About
│  │  │  │  ├─ Bio Section
│  │  │  │  └─ Skills Display ✅ DYNAMIC
│  │  │  │
│  │  │  └─ Contact
│  │  │     ├─ Contact Form
│  │  │     └─ Contact Info ✅ DYNAMIC
│  │  │
│  │  ├─ Auth Routes
│  │  │  ├─ Login
│  │  │  └─ Signup (API only)
│  │  │
│  │  └─ Protected Routes (Admin)
│  │     └─ Dashboard
│  │        ├─ Stats Cards
│  │        ├─ Resume Upload
│  │        ├─ Skills Manager ✅ CRUD
│  │        ├─ Projects Manager ✅ CRUD
│  │        └─ Admin Profile
│  │
│  └─ Footer (All pages)
│
└─ ToastContainer (Global notifications)
```

### Backend Structure

```
server.js
│
├─ Middleware Stack
│  ├─ CORS
│  ├─ Body Parser
│  ├─ Cookie Parser
│  └─ Static Files
│
├─ Routes
│  │
│  ├─ /api/auth
│  │  ├─ POST /signup
│  │  ├─ POST /login
│  │  ├─ POST /logout
│  │  └─ GET  /profile (protected)
│  │
│  ├─ /api/projects
│  │  ├─ GET    /            (public)
│  │  ├─ GET    /:id         (public)
│  │  ├─ POST   /            (admin)
│  │  ├─ PUT    /:id         (admin)
│  │  └─ DELETE /:id         (admin)
│  │
│  └─ /api/about
│     ├─ GET    /            (public)
│     ├─ PUT    /            (admin)
│     ├─ POST   /skills      (admin) ✅
│     ├─ DELETE /skills      (admin) ✅
│     ├─ PUT    /resume      (admin)
│     ├─ GET    /resume      (public)
│     └─ GET    /resume/download (public)
│
├─ Controllers (Business Logic)
│  ├─ authController
│  ├─ projectController
│  └─ aboutController ✅ Skills CRUD
│
├─ Models (Data Schema)
│  ├─ User
│  ├─ Project
│  └─ About ✅ Includes skills & contact
│
├─ Middlewares
│  ├─ auth.js (JWT verify)
│  ├─ adminOnly.js (Role check)
│  └─ upload.js (Multer config)
│
└─ Config
   └─ db.js (MongoDB connection)
```

---

## Security Architecture

```
┌──────────────────────────────────────────────────────┐
│                   Security Layers                     │
└──────────────────────────────────────────────────────┘

Layer 1: CORS Protection
┌────────────────────────┐
│ Only allow requests    │
│ from CLIENT_URL        │
│ Credentials: true      │
└────────────────────────┘
          │
          ▼
Layer 2: JWT Authentication
┌────────────────────────┐
│ Verify JWT token       │
│ Check expiration       │
│ Decode user info       │
└────────────────────────┘
          │
          ▼
Layer 3: Role Authorization
┌────────────────────────┐
│ Check user.role        │
│ admin? → Allow         │
│ user?  → Deny          │
│ none?  → Deny          │
└────────────────────────┘
          │
          ▼
Layer 4: Input Validation
┌────────────────────────┐
│ Validate request body  │
│ Sanitize inputs        │
│ Check file types       │
└────────────────────────┘
          │
          ▼
Layer 5: Database Security
┌────────────────────────┐
│ Mongoose validation    │
│ Password hashing       │
│ No SQL injection       │
└────────────────────────┘
```

---

## State Management (Redux)

```
Redux Store
│
└─ authSlice
   │
   ├─ State
   │  ├─ user: { name, email, role, ... }
   │  ├─ token: "jwt_token_here"
   │  └─ isAuthenticated: boolean
   │
   ├─ Actions
   │  ├─ setUser(user)
   │  ├─ setToken(token)
   │  ├─ logout()
   │  └─ updateUser(user)
   │
   └─ Selectors
      ├─ selectUser
      ├─ selectToken
      └─ selectIsAuthenticated
```

---

## File Upload Architecture

```
Frontend
│
├─ File Input
│  └─ Accept: .pdf only
│
├─ Validation
│  ├─ Type: application/pdf
│  └─ Size: < 5MB
│
└─ FormData
   └─ append('resume', file)
       │
       │ POST /api/about/resume
       ▼
Backend
│
├─ Multer Middleware
│  ├─ Destination: /uploads
│  ├─ Filename: timestamp-original
│  └─ Limits: 5MB
│
├─ Controller
│  ├─ Validate file exists
│  ├─ Delete old resume
│  ├─ Save new path
│  └─ Update database
│
└─ Static Server
   └─ Serve: /uploads/file.pdf
```

---

## API Request/Response Flow

```
Example: Add Skill

Request:
POST /api/about/skills
Authorization: Bearer <jwt_token>
Content-Type: application/json

{
  "skill": "React.js"
}

      │
      ▼
Middleware Chain:
1. CORS Check ✓
2. Body Parser ✓
3. JWT Verify ✓
4. Admin Check ✓
      │
      ▼
Controller:
1. Extract skill from body
2. Validate not empty
3. Find About document
4. Check not duplicate
5. Push to skills array
6. Save document
      │
      ▼
Response:
{
  "success": true,
  "message": "Skill added successfully",
  "data": {
    "about": {
      "_id": "...",
      "skills": ["JavaScript", "React.js", ...],
      ...
    }
  }
}
```

---

## Deployment Architecture

```
Production Setup

┌─────────────────────┐
│   Vercel/Netlify    │  Frontend
│   (Static Hosting)  │  • Build: npm run build
└──────────┬──────────┘  • Deploy: dist/
           │
           │ HTTPS
           │
┌──────────▼──────────┐
│  Railway/Render     │  Backend
│  (Node.js Hosting)  │  • Port: 5000
└──────────┬──────────┘  • Env vars
           │
           │ MongoDB Connection String
           │
┌──────────▼──────────┐
│  MongoDB Atlas      │  Database
│  (Cloud Database)   │  • Cluster
└─────────────────────┘  • Backups
                         • Security
```

---

## Error Handling Flow

```
Error Occurs
     │
     ▼
┌─────────────────┐
│ Try-Catch Block │
└────────┬────────┘
         │
         ├─── Backend ────────┐
         │                    │
         │ Catch Error        │
         │ Log to console     │
         │ Send JSON:         │
         │ {                  │
         │   success: false   │
         │   message: "..."   │
         │ }                  │
         │                    │
         │                    │
         └─── Frontend ───────┤
                             │
         Catch in .catch()   │
         Show toast error    │
         Update UI state     │
         Log to console      │
                             │
                             ▼
                    ┌────────────────┐
                    │  User Feedback │
                    │  (Toast/Alert) │
                    └────────────────┘
```

---

This architecture ensures:
- ✅ Scalable design
- ✅ Secure data flow
- ✅ Clean separation of concerns
- ✅ Easy maintenance
- ✅ Production-ready structure
